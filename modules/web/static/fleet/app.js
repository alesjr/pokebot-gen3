const root=document.querySelector('#fleet'),online=document.querySelector('#online');const esc=x=>String(x??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let streams=new Map();function render(data){const instances=data.instances||[];online.textContent=`${instances.filter(x=>x.status==='running').length} / ${instances.length} online`;root.innerHTML=instances.map(x=>`<article class="instance ${x.status==='running'?'':'offline'}"><img data-profile="${esc(x.profile)}" alt="Tela ${esc(x.game)}"><div class="info"><span class="badge">${esc(x.status)}</span><h2>${esc(x.game)}</h2><dl><dt>Perfil</dt><dd>${esc(x.profile)}</dd><dt>Objetivo</dt><dd>${esc(x.objective)}</dd><dt>Frame</dt><dd>${esc(x.emulator_frame)}</dd><dt>Lease</dt><dd>${esc(x.lease_owner)}</dd></dl></div></article>`).join('');syncStreams()}function syncStreams(){document.querySelectorAll('img[data-profile]').forEach(img=>{const p=img.dataset.profile;if(!document.hidden&&!img.src)img.src=`/fleet/video/${encodeURIComponent(p)}?t=${Date.now()}`;if(document.hidden)img.removeAttribute('src')})}async function refresh(){try{render(await fetch('/fleet/state',{cache:'no-store'}).then(r=>r.json()))}catch(e){online.textContent='coordenador offline'}}document.addEventListener('visibilitychange',syncStreams);refresh();setInterval(refresh,3000);
