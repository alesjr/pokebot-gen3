{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "PokeBot Docker Debug",
            "type": "shell",
            "command": "POKEBOT_DEBUGGER=vscode docker compose up --build --force-recreate -d --wait",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}
