{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "PokeBot Docker Debug",
            "type": "debugpy",
            "request": "attach",
            "connect": {
                "host": "localhost",
                "port": 5679
            },
            "pathMappings": [
                {
                    "localRoot": "${workspaceFolder}",
                    "remoteRoot": "/app"
                }
            ],
            "preLaunchTask": "PokeBot Docker Debug",
            "justMyCode": false
        }
    ]
}
